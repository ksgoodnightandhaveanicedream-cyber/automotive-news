# プロジェクト概要

このプロジェクトは、自動車業界への転職に向けて、自動車業界全般および SDV（Software Defined Vehicle）についてリサーチを行うためのものです。

## 応答ルール

- 常に日本語で簡潔に返答すること。

## 単語帳(自動車用語)の運用

- `docs/tango.html`(一覧・検索表示)と`docs/data/words.js`(用語データ)で構成される。ニュース一覧画面(`docs/index.html`)からリンクしている。
- 会話中にユーザーへ自動車業界・SDV関連の専門用語の意味を説明したときは、その用語がまだ`docs/data/words.js`のWORDS配列に登録されていなければ、末尾に`{ term: "用語", meaning: "意味", category: "カテゴリ名" }`の形式で追加する(重複登録はしない)。`category`は既存のいずれか(SDV・ソフトウェア / 電動化・EV / 自動運転・ADAS / 業界動向・ビジネス / 車両技術 / 法規・安全基準)に当てはまればそれを使い、どれにも当てはまらない場合のみ新しいカテゴリ名を作る。
- 追加した場合は、その旨を一言ユーザーに伝える。
- `docs/data/words.js`を編集する前に必ず`git pull`を実行し、他の端末(スマホのクラウドセッションなど)で追加された最新の内容を取り込んでから編集する。これを怠ると、他端末での追加分を知らないまま古い内容で上書きしようとして`git push`が拒否される。
- ファイルを更新したら、確認を挟まず`git add`→`git commit`→`git push`を行い、GitHub Pages上の公開版に反映してよい(コミット対象は単語帳関連の変更のみとし、他の未関連な変更は含めない)。`git push`が拒否された場合は、`git pull`(必要なら`--rebase`)で最新内容を取り込み、再度編集内容が正しく反映されているか確認してから push し直す。
